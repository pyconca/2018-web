{% extends 'base.tmpl' %}

{% block content_header %}
<div class="container mt-4">
  <div class="page-title border-bottom-1 pb-1 mb-4">
    <h1 class="display-1">{{ post.title()|e }}</h1>
    {# <h2 class="h1">{{ post.subtitle()|e }}</h2> #}
  </div>
</div>
{% endblock %}

{% block content_post %}
<div class="container">
  {{ post.text() }}
</div>
{% endblock %}
